Start fresh from the latest main branch after a merge.

Run these git commands in order:
1. `git checkout main`
2. `git pull origin main`

Report the current branch and latest commit hash/message once done so the user can confirm they are up to date.
